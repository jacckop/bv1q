# Changelog

## 7.0.0 — Translation Display Path Restore

- مقارنة منطق v3 مع v6.1 وإعادة مسار التعريب الأصلي عبر `NSBundle`.
- تقييد اعتراض `NSBundle` إلى المطابقة الكاملة من القاموس فقط، بدون قواعد Regex أو ترجمة جزئية.
- إصلاح شرط نوع الـSwift ivar: السماح بترميز فارغ لعقدة `attributedText` المعروفة بدلاً من رفضها دائماً.
- الإبقاء على الفحص المدمج والمحدود السرعة الذي أصلح تعليق إعدادات تيليجرام.
- عدم إعادة اعتراض `UITextView` لحماية نصوص الرسائل والمحتوى القابل للتحرير.
- الإبقاء على 988 مدخلاً في القاموس و593/593 من النصوص المستخرجة.

## 6.1.0 — ARC Build Fix

- إصلاح فشل GitHub Actions في `Sources/Tweak.m` عند السطر 81.
- حذف مسار `objc_storeStrong` الذي كان يحوّل مؤشراً خاماً إلى `id *`، وهو غير مسموح مع `-fobjc-arc`.
- استخدام `object_setIvarWithStrongDefault` عند توفره، ثم `object_setIvar` كمسار احتياطي قابل للبناء.
- الإبقاء على إصلاح كرش إعدادات تيليجرام وعلى قاموس التعريب الكامل.

## 6.0.0 — Texture Translation Restore

- إصلاح اختفاء التعريب داخل صفحة Whitegram وخياراتها بعد إصلاح الكرش في v5.
- السبب: v5 كان يلغي الكتابة داخل عُقد Texture بالكامل إذا لم تتوفر الدالة الخاصة `object_setIvarWithStrongDefault`.
- إضافة مسار كتابة متدرج: الدالة الخاصة عند توفرها، ثم `objc_storeStrong`، ثم `object_setIvar` كحل أخير.
- الإبقاء على نظام الفحص المدمج والمحدود السرعة من v5 لمنع عودة تعليق إعدادات تيليجرام.
- الإبقاء على المطابقة الكاملة للنص وعلى فحص نوع وحدود الـ ivar قبل تعديله.
- لا إعادة لاعتراض `NSBundle` أو `UITextView` العام.

## 5.0.0 — Telegram Settings Crash Fix

- إصلاح التعليق والكرش عند فتح **إعدادات تيليجرام**.
- كان الإصدار v4 يشغّل خمس عمليات فحص كاملة لكل `_ASDisplayView` يدخل النافذة؛ صفحة إعدادات تيليجرام تنشئ عدداً كبيراً من هذه العناصر، فتتكدس مئات عمليات الفحص على الخيط الرئيسي.
- استبدال دفعات الفحص بنظام واحد مدمج ومحدود السرعة، بحيث تتجمع الاستدعاءات المتكررة في عملية فحص واحدة فقط.
- إزالة اعتراض `NSBundle` العام لأنه قد يغيّر نصوصاً تستخدمها تيليجرام داخلياً قبل عرضها.
- إزالة اعتراض `UITextView` العام حتى لا يمس محتوى الرسائل أو النصوص التي يكتبها المستخدم.
- تقييد تعديل عُقد Swift/Texture إلى مطابقة قاموس كاملة ونص ذي تنسيق موحد، مع التوقف إذا لم تتوفر دالة الكتابة الآمنة للـ ivar.
- تقليل حدود عمق وعدد العُقد المفحوصة لمنع الضغط على الواجهة.
- الإبقاء على قاموس v4 الكامل المتوافق مع Whitegram 12.9.2 (60).

## 4.0.0 — Whitegram 12.9.2 Update

- تحديث القاموس لنسخة Whitegram 12.9.2 (60).
- إضافة 246 ترجمة جديدة تشمل الخيارات والعناوين والوصف متعدد الأسطر والتنبيهات.
- تحديث Bundle ID إلى `ph.telegra.Telegraph` مع التوافق مع البنايات السابقة.

## 3.0.0 — NodeFix

- إضافة تعريب لعُقد AsyncDisplayKit/Texture وواجهات ComponentFlow.Text.
